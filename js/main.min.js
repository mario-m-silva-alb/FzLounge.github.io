 function trackEvent(eventName,parameters={}){if(typeof gtag==='function'){gtag('event',eventName,parameters);}}function basePath(){const loc=window.location.pathname;return loc.substring(0,loc.lastIndexOf('/')+ 1);}async function loadData(){const url=basePath()+ 'data/products.json';const res=await fetch(url);if(!res.ok)throw new Error(`Failed to load products.json(${res.status})`);return res.json();}const CONDITION_LABELS={'sealed': 'Sealed',};const CATEGORY_LABELS={'single': 'Single Card','deck': 'Deck','booster': 'Booster Pack','accessory': 'Accessory',};const STATUS_LABELS={'presale': 'Presale','available': 'In Stock','limited': 'Limited','sold': 'Sold Out'};const STATUS_ICONS={'presale': 'ðŸŽ','available': 'ðŸ“¦','limited': 'âš¡','sold': 'ðŸ”’'};function buildCardHTML(product,games,titleTag){const tag=titleTag || 'h2';const gameLabel=(games.find(g=> g.value===product.game)||{}).label || product.game;const condLabel=CONDITION_LABELS[product.condition] || product.condition;const condClass=`badge-${product.condition}`;const catLabel=CATEGORY_LABELS[product.category] || product.category;const escapedName=product.name.replace(/"/g,'&quot;');const status=product.status || 'available';const statusLabel=STATUS_LABELS[status] || status;const statusIcon=STATUS_ICONS[status] || '';const statusClass=`badge-status badge-status-${status}`;const isNew=product.dateAdded && isProductNew(product.dateAdded);const newBadge=isNew ? '<span class="badge-new">âœ¨ NEW</span>' : '';const productId=`#${String(product.id).padStart(3,'0')}`;const imageSrc=product.image;const imageFallback=product.imageFallback || product.image;const imageHTML=imageSrc.endsWith('.webp')? `<picture> <source srcset="${imageSrc}" type="image/webp"> <img src="${imageFallback}" alt="${product.imageAlt}" loading="lazy" /> </picture>` : `<img src="${imageSrc}" alt="${product.imageAlt}" loading="lazy" />`;let pricingHTML='';if(product.prices){const minPrice=Math.min(product.prices.wood || Infinity,product.prices.bronze || Infinity,product.prices.silver || Infinity,product.prices.gold || Infinity,product.prices.platinum || Infinity);pricingHTML=` <div class="product-card__pricing"> <span class="price-from">From â‚¬${minPrice.toFixed(2)}</span> <span class="price-tiers">Member pricing</span> </div> `;}return ` <article class="product-card" data-game="${product.game}" data-condition="${product.condition}" data-category="${product.category}" data-status="${status}" data-product-id="${product.id}" data-search-text="${escapedName.toLowerCase()}${product.description.toLowerCase()}${gameLabel.toLowerCase()}"> <div class="product-card__img-wrap"> ${imageHTML}<span class="product-card__badge ${condClass}">${condLabel}</span> <span class="product-card__game-tag">${gameLabel}</span> <div class="product-card__badges"> <span class="${statusClass}">${statusIcon}${statusLabel}</span> ${newBadge}</div> </div> <div class="product-card__body"> <${tag}class="product-card__title">${product.name}</${tag}> <p class="product-card__desc">${product.description}</p> ${pricingHTML}<div class="product-card__footer"> <span class="product-card__category">${catLabel}</span> <span class="product-card__id">${productId}</span> <button class="btn btn-primary btn-sm btn-contact" data-product="${escapedName}">Contact to Get</button> </div> </div> </article>`.trim();}function isProductNew(dateAdded){const added=new Date(dateAdded);const now=new Date();const daysDiff=(now - added)/(1000 * 60 * 60 * 24);return daysDiff <=30;}function populateGameSelects(games){document.querySelectorAll('[data-game-select]').forEach(select=>{const type=select.dataset.gameSelect;games.forEach(game=>{const opt=document.createElement('option');opt.value=game.value;opt.textContent=game.label;select.appendChild(opt);});if(type==='filter'){const all=document.createElement('option');all.value='all';all.textContent='All Games';select.insertBefore(all,select.firstChild);}});}function generateProductStructuredData(products){if(!document.getElementById('products-grid'))return;const existingScript=document.querySelector('script[data-product-schema]');if(existingScript){existingScript.remove();}const itemListSchema={"@context": "https: "@type": "ItemList","itemListElement": products.map((product,index)=>({"@type": "ListItem","position": index + 1,"item":{"@type": "Product","name": product.name,"image": product.image,"description": product.description || `${product.name}- ${product.game}trading card product`,"category": product.category,"brand":{"@type": "Brand","name": getBrandName(product.game)},"offers":{"@type": "Offer","availability": getSchemaAvailability(product.status || 'available'),"itemCondition": "https: "url": `https:}}}))};const script=document.createElement('script');script.type='application/ld+json';script.setAttribute('data-product-schema','true');script.textContent=JSON.stringify(itemListSchema,null,2);document.head.appendChild(script);}function getBrandName(game){const brandMap={'mtg': 'Wizards of the Coast','pokemon': 'The PokÃ©mon Company','yugioh': 'Konami','lorcana': 'Ravensburger','onepiece': 'Bandai','digimon': 'Bandai','unionarena': 'Bushiroad','weissschwarz': 'Bushiroad','dragonball': 'Bandai','gundam': 'Bandai','riftbound': 'Riftbound Games','starwars': 'Fantasy Flight Games','fleshandblood': 'Legend Story Studios'};return brandMap[game] || 'FzLounge';}function getSchemaAvailability(status){const availabilityMap={'available': 'https: 'presale': 'https: 'limited': 'https: 'sold': 'https:};return availabilityMap[status] || 'https:}function initProductsPage(data){const grid=document.getElementById('products-grid');if(!grid)return;let currentProducts=[...data.products];function renderProducts(products){const cards=products .map(p=> buildCardHTML(p,data.games,'h2')).join('\n');grid.innerHTML='<div id="no-results" class="no-results" style="display:none;" role="status"><div class="icon" aria-hidden="true">ðŸ”</div><p>No products match your filters. Try resetting them!</p></div>' + cards;attachProductCardHandlers(data);}renderProducts(currentProducts);generateProductStructuredData(data.products);const filterGame=document.getElementById('filter-game');const filterCategory=document.getElementById('filter-category');const filterStatus=document.getElementById('filter-status');const sortSelect=document.getElementById('sort-products');const filterResetBtn=document.getElementById('filter-reset');const productsCount=document.getElementById('products-count');const searchInput=document.getElementById('product-search');const searchClear=document.getElementById('search-clear');const activeFiltersDiv=document.getElementById('active-filters');const filterChipsDiv=document.getElementById('filter-chips');let searchTerm='';let searchTimeout=null;function updateFilterChips(){if(!filterChipsDiv || !activeFiltersDiv)return;const chips=[];const game=filterGame?.value;const category=filterCategory?.value;const status=filterStatus?.value;if(game && game !=='all'){const gameLabel=data.games.find(g=> g.value===game)?.label || game;chips.push({type: 'game',label: `Game: ${gameLabel}`,value: game});}if(category && category !=='all'){const catLabel=CATEGORY_LABELS[category] || category;chips.push({type: 'category',label: `Category: ${catLabel}`,value: category});}if(status && status !=='all'){const statusLabel=STATUS_LABELS[status] || status;chips.push({type: 'status',label: `Status: ${statusLabel}`,value: status});}if(searchTerm){chips.push({type: 'search',label: `Search: "${searchTerm}"`,value: searchTerm});}if(chips.length===0){activeFiltersDiv.style.display='none';return;}activeFiltersDiv.style.display='flex';filterChipsDiv.innerHTML=chips.map(chip=> `<button class="filter-chip" data-filter-type="${chip.type}" data-filter-value="${chip.value}"> ${chip.label}<svg xmlns="http: <line x1="18" y1="6" x2="6" y2="18"></line> <line x1="6" y1="6" x2="18" y2="18"></line> </svg> </button>`).join('');filterChipsDiv.querySelectorAll('.filter-chip').forEach(chip=>{chip.addEventListener('click',()=>{const type=chip.dataset.filterType;if(type==='game' && filterGame)filterGame.value='all';if(type==='category' && filterCategory)filterCategory.value='all';if(type==='status' && filterStatus)filterStatus.value='all';if(type==='search' && searchInput){searchInput.value='';searchTerm='';if(searchClear)searchClear.style.display='none';}applyFilters();});});}function sortProducts(products,sortBy){const sorted=[...products];switch(sortBy){case 'newest': sorted.sort((a,b)=>{const dateA=new Date(a.dateAdded || '2000-01-01');const dateB=new Date(b.dateAdded || '2000-01-01');return dateB - dateA;});break;case 'name-asc': sorted.sort((a,b)=> a.name.localeCompare(b.name));break;case 'name-desc': sorted.sort((a,b)=> b.name.localeCompare(a.name));break;case 'game': sorted.sort((a,b)=>{const gameA=data.games.find(g=> g.value===a.game)?.label || a.game;const gameB=data.games.find(g=> g.value===b.game)?.label || b.game;return gameA.localeCompare(gameB);});break;case 'featured': default: sorted.sort((a,b)=>{if(a.featured && !b.featured)return -1;if(!a.featured && b.featured)return 1;const dateA=new Date(a.dateAdded || '2000-01-01');const dateB=new Date(b.dateAdded || '2000-01-01');return dateB - dateA;});break;}return sorted;}function applyFilters(){const game=filterGame ? filterGame.value : 'all';const category=filterCategory ? filterCategory.value : 'all';const status=filterStatus ? filterStatus.value : 'all';const sortBy=sortSelect ? sortSelect.value : 'featured';const search=searchTerm.toLowerCase().trim();let filteredProducts=currentProducts.filter(product=>{const matchGame=game==='all' || product.game===game;const matchCategory=category==='all' || product.category===category;const matchStatus=status==='all' ||(product.status || 'available')===status;const searchText=`${product.name}${product.description}${data.games.find(g=> g.value===product.game)?.label || ''}`.toLowerCase();const matchSearch=!search || searchText.includes(search);return matchGame && matchCategory && matchStatus && matchSearch;});filteredProducts=sortProducts(filteredProducts,sortBy);renderProducts(filteredProducts);let visible=0;grid.querySelectorAll('.product-card[data-game]').forEach(card=>{if(card.style.display !=='none')visible++;});if(productsCount){const filterText=search ? ` for "${searchTerm}"` : '';productsCount.textContent=`${visible}product${visible !==1 ? 's' : ''}found${filterText}`;}const noResults=document.getElementById('no-results');if(noResults){noResults.style.display=visible===0 ? 'block' : 'none';}updateFilterChips();if(search){trackEvent('search',{'search_term': searchTerm});}if(game !=='all' || category !=='all' || status !=='all'){trackEvent('filter_products',{'game': game,'category': category,'status': status});}}function handleSearch(value){searchTerm=value;if(searchClear){searchClear.style.display=searchTerm ? 'flex' : 'none';}clearTimeout(searchTimeout);searchTimeout=setTimeout(()=>{applyFilters();},300);}if(filterGame)filterGame.addEventListener('change',applyFilters);if(filterCategory)filterCategory.addEventListener('change',applyFilters);if(filterStatus)filterStatus.addEventListener('change',applyFilters);if(sortSelect)sortSelect.addEventListener('change',applyFilters);if(searchInput){searchInput.addEventListener('input',(e)=> handleSearch(e.target.value));searchInput.addEventListener('search',applyFilters);}if(searchClear){searchClear.addEventListener('click',()=>{if(searchInput){searchInput.value='';handleSearch('');}});}if(filterResetBtn){filterResetBtn.addEventListener('click',()=>{if(filterGame)filterGame.value='all';if(filterCategory)filterCategory.value='all';if(filterStatus)filterStatus.value='all';if(sortSelect)sortSelect.value='featured';if(searchInput){searchInput.value='';searchTerm='';if(searchClear)searchClear.style.display='none';}applyFilters();});}applyFilters();}function initIndexPage(data){const grid=document.getElementById('featured-grid');if(!grid)return;const featured=data.products.filter(p=> p.featured);grid.innerHTML=featured.map(p=> buildCardHTML(p,data.games,'h3')).join('\n');attachProductCardHandlers(data);}const DISCORD_URL='https: const WHATSAPP_URL='https: function injectContactModal(){if(document.getElementById('contact-modal'))return;const modal=document.createElement('div');modal.id='contact-modal';modal.className='modal-overlay';modal.setAttribute('role','dialog');modal.setAttribute('aria-modal','true');modal.setAttribute('aria-labelledby','modal-title');modal.style.display='none';modal.innerHTML=` <div class="modal"> <button class="modal-close" id="modal-close" aria-label="Close">âœ•</button> <h2 id="modal-title" class="modal-title">Contact to Get</h2> <p class="modal-desc">Interested in <strong id="modal-product-name"></strong>? Reach out on one of our community channels:</p> <div class="modal-channels"> <a href="${DISCORD_URL}" target="_blank" rel="noopener noreferrer" class="btn btn-primary modal-channel-btn" id="contact-discord"> ðŸ’¬ Discord </a> <a href="${WHATSAPP_URL}" target="_blank" rel="noopener noreferrer" class="btn btn-outline modal-channel-btn" id="contact-whatsapp"> ðŸ“± WhatsApp </a> </div> </div>`;document.body.appendChild(modal);document.getElementById('modal-close').addEventListener('click',closeContactModal);modal.addEventListener('click',(e)=>{if(e.target===modal)closeContactModal();});document.addEventListener('keydown',(e)=>{if(e.key==='Escape')closeContactModal();});document.getElementById('contact-discord').addEventListener('click',()=>{const productName=document.getElementById('modal-product-name').textContent;trackEvent('contact_click',{'platform': 'discord','product_name': productName});});document.getElementById('contact-whatsapp').addEventListener('click',()=>{const productName=document.getElementById('modal-product-name').textContent;trackEvent('contact_click',{'platform': 'whatsapp','product_name': productName});});}function openContactModal(productName){const modal=document.getElementById('contact-modal');if(!modal)return;document.getElementById('modal-product-name').textContent=productName;modal.style.display='flex';document.body.style.overflow='hidden';document.getElementById('modal-close').focus();trackEvent('contact_modal_open',{'product_name': productName});}function closeContactModal(){const modal=document.getElementById('contact-modal');if(!modal)return;modal.style.display='none';document.body.style.overflow='';}function initContactButtons(){injectContactModal();document.addEventListener('click',(e)=>{const btn=e.target.closest('.btn-contact');if(!btn)return;openContactModal(btn.dataset.product || 'this product');});}let productsData=null;let currentProductIndex=-1;function attachProductCardHandlers(data){productsData=data;document.querySelectorAll('.product-card').forEach(card=>{card.style.cursor='pointer';card.addEventListener('click',(e)=>{if(e.target.closest('.btn-contact'))return;const productId=parseInt(card.dataset.productId);openProductDetail(productId);});});}function buildProductDetailHTML(product,games){const gameLabel=(games.find(g=> g.value===product.game)||{}).label || product.game;const condLabel=CONDITION_LABELS[product.condition] || product.condition;const catLabel=CATEGORY_LABELS[product.category] || product.category;const status=product.status || 'available';const statusLabel=STATUS_LABELS[status] || status;const statusIcon=STATUS_ICONS[status] || '';const isNew=product.dateAdded && isProductNew(product.dateAdded);const productId=`#${String(product.id).padStart(3,'0')}`;let preorderDateHTML='';if(status==='presale' && product.preorderDate){const preorderDate=new Date(product.preorderDate).toLocaleDateString('en-US',{year: 'numeric',month: 'long',day: 'numeric'});preorderDateHTML=`<div class="spec-item"><span class="spec-label">Preorder Opened:</span> <span class="spec-value">${preorderDate}</span></div>`;}let releaseDateHTML='';if(status==='presale' && product.releaseDate){const releaseDate=new Date(product.releaseDate).toLocaleDateString('en-US',{year: 'numeric',month: 'long',day: 'numeric'});releaseDateHTML=`<div class="spec-item"><span class="spec-label">Release Date:</span> <span class="spec-value">${releaseDate}</span></div>`;}const dateAdded=product.dateAdded ? new Date(product.dateAdded).toLocaleDateString('en-US',{year: 'numeric',month: 'long',day: 'numeric'}): 'Unknown';const relatedProducts=productsData.products .filter(p=> p.id !==product.id &&(p.game===product.game || p.category===product.category)).slice(0,4);const relatedHTML=relatedProducts.length > 0 ? ` <div class="modal-related"> <h3>Related Products</h3> <div class="related-products-grid"> ${relatedProducts.map(p=>{const relGameLabel=(games.find(g=> g.value===p.game)||{}).label || p.game;return ` <div class="related-product" data-product-id="${p.id}"> <img src="${p.image}" alt="${p.imageAlt}" loading="lazy" /> <div class="related-product__info"> <h4>${p.name}</h4> <span class="related-product__game">${relGameLabel}</span> </div> </div> `;}).join('')}</div> </div> ` : '';return ` <div class="modal-product-detail"> <div class="modal-product-image"> <img src="${product.image}" alt="${product.imageAlt}" /> ${isNew ? '<span class="badge-new modal-badge">âœ¨ NEW</span>' : ''}</div> <div class="modal-product-info"> <h2 id="modal-product-title">${product.name}</h2> <div class="modal-product-meta"> <span class="badge-status badge-status-${status}">${statusIcon}${statusLabel}</span> <span class="product-id">${productId}</span> </div> <p class="modal-product-description">${product.description}</p> <div class="modal-product-specs"> <h3>Specifications</h3> <div class="spec-item"> <span class="spec-label">Game:</span> <span class="spec-value">${gameLabel}</span> </div> <div class="spec-item"> <span class="spec-label">Category:</span> <span class="spec-value">${catLabel}</span> </div> <div class="spec-item"> <span class="spec-label">Condition:</span> <span class="spec-value">${condLabel}</span> </div> ${preorderDateHTML}${releaseDateHTML}<div class="spec-item"> <span class="spec-label">Added:</span> <span class="spec-value">${dateAdded}</span> </div> </div> ${buildTierPricingHTML(product)}<div class="modal-product-actions"> <button class="btn btn-primary btn-lg btn-contact" data-product="${product.name}"> Contact to Get </button> <button class="btn btn-outline btn-share" id="btn-share-product"> <svg xmlns="http: <rect x="9" y="9" width="13" height="13" rx="2" ry="2"></rect> <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"></path> </svg> Copy Name </button> </div> </div> </div> ${relatedHTML}`;}function attachProductCardHandlers(data){productsData=data;document.querySelectorAll('.product-card').forEach(card=>{card.style.cursor='pointer';card.addEventListener('click',(e)=>{if(e.target.closest('.btn-contact'))return;const productId=parseInt(card.dataset.productId);openProductDetail(productId);});});}function buildTierPricingHTML(product){if(!product.prices)return '';const tiers=[{id: 'wood',name: 'Wood',icon: 'ðŸŒ³'},{id: 'bronze',name: 'Bronze',icon: 'ðŸ¥‰'},{id: 'silver',name: 'Silver',icon: 'ðŸ¥ˆ'},{id: 'gold',name: 'Gold',icon: 'ðŸ¥‡'},{id: 'platinum',name: 'Platinum',icon: 'ðŸ’Ž'}];const basePrice=product.prices.wood;return ` <div class="modal-product-pricing"> <h3>Membership Pricing</h3> <div class="pricing-table"> ${tiers.map(tier=>{const price=product.prices[tier.id];if(price===undefined)return '';const savings=basePrice - price;const savingsText=savings > 0 ? `<span class="savings-badge">Save â‚¬${savings.toFixed(2)}</span>` : '';return ` <div class="pricing-row tier-${tier.id}"> <span class="tier-info"> <span class="tier-icon">${tier.icon}</span> <span class="tier-name">${tier.name}</span> </span> <span class="tier-price-group"> <span class="tier-price">â‚¬${price.toFixed(2)}</span> ${savingsText}</span> </div> `;}).join('')}</div> <a href="tiers.html" class="tier-info-link">Learn about membership tiers â†’</a> </div> `;}function openProductDetail(productId){if(!productsData)return;const product=productsData.products.find(p=> p.id===productId);if(!product)return;currentProductIndex=productsData.products.findIndex(p=> p.id===productId);const modal=document.getElementById('product-detail-modal');const modalBody=document.getElementById('modal-detail-body');if(!modal || !modalBody)return;modalBody.innerHTML=buildProductDetailHTML(product,productsData.games);modal.style.display='flex';document.body.style.overflow='hidden';window.location.hash=`product-${productId}`;trackEvent('view_item',{'items': [{'item_id': product.id,'item_name': product.name,'item_category': product.game,'item_category2': product.category}]});const shareBtn=document.getElementById('btn-share-product');if(shareBtn){shareBtn.addEventListener('click',()=> shareProduct(productId));}modalBody.querySelectorAll('.related-product').forEach(relCard=>{relCard.style.cursor='pointer';relCard.addEventListener('click',()=>{const relProductId=parseInt(relCard.dataset.productId);openProductDetail(relProductId);});});updateModalNavButtons();}function closeProductDetail(){const modal=document.getElementById('product-detail-modal');if(!modal)return;modal.style.display='none';document.body.style.overflow='';if(window.location.hash.startsWith('#product-')){history.pushState('',document.title,window.location.pathname + window.location.search);}}function navigateProduct(direction){if(!productsData || currentProductIndex < 0)return;const newIndex=direction==='prev' ?(currentProductIndex - 1 + productsData.products.length)% productsData.products.length :(currentProductIndex + 1)% productsData.products.length;const newProduct=productsData.products[newIndex];if(newProduct){openProductDetail(newProduct.id);}}function updateModalNavButtons(){const prevBtn=document.getElementById('modal-nav-prev');const nextBtn=document.getElementById('modal-nav-next');if(!productsData || !prevBtn || !nextBtn)return;prevBtn.disabled=false;nextBtn.disabled=false;}function shareProduct(productId){if(!productsData)return;const product=productsData.products.find(p=> p.id===productId);if(!product)return;const productName=product.name;const productIdFormatted=`#${String(product.id).padStart(3,'0')}`;const textToCopy=`${productName}(${productIdFormatted})`;if(navigator.clipboard && navigator.clipboard.writeText){navigator.clipboard.writeText(textToCopy).then(()=>{const shareBtn=document.getElementById('btn-share-product');if(shareBtn){const originalText=shareBtn.innerHTML;shareBtn.innerHTML='<svg xmlns="http: shareBtn.style.pointerEvents='none';setTimeout(()=>{shareBtn.innerHTML=originalText;shareBtn.style.pointerEvents='';},2000);}}).catch(err=>{console.error('Failed to copy product name:',err);alert('Could not copy to clipboard');});}else{alert(`Product name: ${textToCopy}\n\nCopy this and send to us!`);}}function initProductDetailModal(){const modal=document.getElementById('product-detail-modal');if(!modal)return;const closeBtn=document.getElementById('modal-detail-close');if(closeBtn){closeBtn.addEventListener('click',closeProductDetail);}modal.addEventListener('click',(e)=>{if(e.target===modal){closeProductDetail();}});const prevBtn=document.getElementById('modal-nav-prev');const nextBtn=document.getElementById('modal-nav-next');if(prevBtn){prevBtn.addEventListener('click',()=> navigateProduct('prev'));}if(nextBtn){nextBtn.addEventListener('click',()=> navigateProduct('next'));}document.addEventListener('keydown',(e)=>{if(modal.style.display !=='flex')return;switch(e.key){case 'Escape': closeProductDetail();break;case 'ArrowLeft': navigateProduct('prev');break;case 'ArrowRight': navigateProduct('next');break;}});if(window.location.hash.startsWith('#product-')){const productId=parseInt(window.location.hash.replace('#product-',''));if(!isNaN(productId)){setTimeout(()=> openProductDetail(productId),100);}}}function animateStatsCounters(){const statNumbers=document.querySelectorAll('.stat-number');if(statNumbers.length===0)return;const observerOptions={threshold: 0.5,rootMargin: '0px'};const observer=new IntersectionObserver((entries)=>{entries.forEach(entry=>{if(entry.isIntersecting && !entry.target.classList.contains('animated')){entry.target.classList.add('animated');animateCounter(entry.target);}});},observerOptions);statNumbers.forEach(stat=> observer.observe(stat));}function animateCounter(element){const target=parseInt(element.getAttribute('data-target'));const duration=2000;const increment=target /(duration / 16);let current=0;const timer=setInterval(()=>{current +=increment;if(current >=target){element.textContent=target + '+';clearInterval(timer);}else{element.textContent=Math.floor(current);}},16);}async function loadUpcomingEvents(){const grid=document.getElementById('upcoming-events-grid');if(!grid)return;try{const response=await fetch('data/events.json');if(!response.ok)throw new Error('Failed to load events');const data=await response.json();const now=new Date();const upcoming=data.events .map(event=>{const eventDate=new Date(event.date + 'T' + event.endTime);return{...event,eventDate};}).filter(event=> event.eventDate > now).sort((a,b)=> a.eventDate - b.eventDate).slice(0,3).map(event=> calculateEventStatus(event));if(upcoming.length===0){grid.innerHTML='<p style="text-align:center;color:var(--color-text-muted);">No upcoming events scheduled. Check back soon!</p>';return;}grid.innerHTML=upcoming.map(event=> buildCompactEventCard(event)).join('');}catch(error){console.error('Error loading upcoming events:',error);grid.innerHTML='<p style="text-align:center;color:var(--color-text-muted);">Unable to load events.</p>';}}function calculateEventStatus(event){const eventDate=new Date(event.date + 'T' + event.endTime);const now=new Date();if(eventDate < now){return{...event,calculatedStatus: 'past'};}return{...event,calculatedStatus: event.status};}function buildCompactEventCard(event){const eventDate=new Date(event.date);const month=eventDate.toLocaleDateString('en-US',{month: 'short'});const day=eventDate.getDate();const time=event.time.substring(0,5);const statusConfig={open:{label: 'Open',icon: 'ðŸŸ¢',class: 'status-open'},almost_full:{label: 'Almost Full',icon: 'ðŸŸ¡',class: 'status-almost-full'},full:{label: 'Full',icon: 'ðŸ”´',class: 'status-full'}};const status=statusConfig[event.calculatedStatus] || statusConfig.open;return ` <article class="compact-event-card"> <div class="compact-event-date"> <div class="compact-date-month">${month}</div> <div class="compact-date-day">${day}</div> <div class="compact-date-time">${time}</div> </div> <div class="compact-event-info"> <h3 class="compact-event-title">${event.title}</h3> <div class="compact-event-meta"> <span>ðŸ“… ${eventDate.toLocaleDateString('en-US',{weekday: 'short'})}</span> <span>ðŸ‘¥ Capacity: ${event.capacity}</span> <span>${event.format || 'Various'}</span> </div> <p class="compact-event-description">${event.description}</p> <div class="compact-event-footer"> <span class="compact-event-status ${status.class}"> ${status.icon}${status.label}</span> <a href="events.html" class="btn btn-sm btn-primary">Details â†’</a> </div> </div> </article> `;}function initScrollAnimations(){if(window.matchMedia('(prefers-reduced-motion: reduce)').matches){return;}const observerOptions={threshold: 0.1,rootMargin: '0px 0px -50px 0px'};const observer=new IntersectionObserver((entries)=>{entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('animated');observer.unobserve(entry.target);}});},observerOptions);document.querySelectorAll('.animate-on-scroll').forEach(el=>{observer.observe(el);});}document.addEventListener('DOMContentLoaded',()=>{const navToggle=document.getElementById('nav-toggle');const navLinks=document.getElementById('nav-links');if(navToggle && navLinks){navToggle.addEventListener('click',()=>{const isOpen=navLinks.classList.toggle('open');navToggle.setAttribute('aria-expanded',isOpen);navToggle.textContent=isOpen ? 'âœ•' : 'â˜°';});document.addEventListener('click',(e)=>{if(!navToggle.contains(e.target)&& !navLinks.contains(e.target)){navLinks.classList.remove('open');navToggle.setAttribute('aria-expanded','false');navToggle.textContent='â˜°';}});}const currentPage=window.location.pathname.split('/').pop()|| 'index.html';document.querySelectorAll('.navbar__links a').forEach(link=>{const href=link.getAttribute('href');if(href===currentPage ||(currentPage==='' && href==='index.html')){link.classList.add('active');}});initContactButtons();initProductDetailModal();animateStatsCounters();loadUpcomingEvents();initScrollAnimations();const needsData=document.getElementById('products-grid')|| document.getElementById('featured-grid')|| document.querySelector('[data-game-select]');if(needsData){loadData().then(data=>{populateGameSelects(data.games);initProductsPage(data);initIndexPage(data);}).catch(err=>{console.error('FzLounge: could not load products.json â€“',err);const grid=document.getElementById('products-grid')|| document.getElementById('featured-grid');if(grid){grid.innerHTML='<p class="no-results" style="grid-column:1/-1;text-align:center;padding:2rem;">Unable to load products. Please try again later.</p>';}});}});