 let eventsData=null;let allEvents=[];const STATUS_CONFIG={open:{label: 'Open',icon: 'ðŸŸ¢',class: 'status-open'},almost_full:{label: 'Almost Full',icon: 'ðŸŸ¡',class: 'status-almost-full'},full:{label: 'Full',icon: 'ðŸ”´',class: 'status-full'},past:{label: 'Past Event',icon: 'âš«',class: 'status-past'}};function generateEventStructuredData(events){const existingScript=document.querySelector('script[data-event-schema]');if(existingScript){existingScript.remove();}const upcomingEvents=events.filter(e=> e.calculatedStatus !=='past');const eventSchemas=upcomingEvents.map(event=>({"@context": "https: "@type": "Event","name": event.title,"startDate": `${event.date}T${event.time}`,"endDate": event.endTime ? `${event.date}T${event.endTime}` : undefined,"description": event.description || `${event.title}at FzLounge`,"eventStatus": event.calculatedStatus==='past' ? "https: : "https: "eventAttendanceMode": "https: "location":{"@type": "Place","name": "FzLounge","address":{"@type": "PostalAddress","addressLocality": "Your City","addressCountry": "Your Country"}},"organizer":{"@type": "Organization","name": "FzLounge","url": "https:},"image": event.image || "https: "offers": event.capacity ?{"@type": "Offer","availability": event.calculatedStatus==='full' ? "https: : "https: "price": "0","priceCurrency": "EUR","validFrom": new Date().toISOString().split('T')[0],"url": event.rsvpLink || "https:}: undefined,"maximumAttendeeCapacity": event.capacity || undefined,"remainingAttendeeCapacity": event.capacity && event.registered ? Math.max(0,event.capacity - event.registered): undefined}));const script=document.createElement('script');script.type='application/ld+json';script.setAttribute('data-event-schema','true');script.textContent=JSON.stringify(eventSchemas.length===1 ? eventSchemas[0] : eventSchemas,null,2);document.head.appendChild(script);}document.addEventListener('DOMContentLoaded',()=>{loadEventsData();initNavToggle();initEventDetailModal();});async function loadEventsData(){try{const eventsResponse=await fetch('data/events.json');if(!eventsResponse.ok){throw new Error('Failed to load events data');}eventsData=await eventsResponse.json();const productsResponse=await fetch('data/products.json');if(!productsResponse.ok){throw new Error('Failed to load products data');}const productsData=await productsResponse.json();populateEventTypeFilter();populateGamesFilter(productsData.games);allEvents=eventsData.events.map(calculateEventStatus);allEvents.sort((a,b)=> new Date(a.date)- new Date(b.date));renderEvents(allEvents);updateEventsCount(allEvents.length);generateEventStructuredData(allEvents);setupFilters();}catch(error){console.error('Error loading events:',error);showErrorMessage();}}function calculateEventStatus(event){const eventDate=new Date(event.date + 'T' + event.endTime);const now=new Date();if(eventDate < now){return{...event,calculatedStatus: 'past'};}return{...event,calculatedStatus: event.status};}function populateEventTypeFilter(){const select=document.getElementById('filter-event-type');if(!select || !eventsData)return;eventsData.eventTypes.forEach(type=>{const option=document.createElement('option');option.value=type.value;option.textContent=`${type.icon}${type.label}`;select.appendChild(option);});}function populateGamesFilter(games){const select=document.getElementById('filter-event-game');if(!select || !games)return;games.forEach(game=>{const option=document.createElement('option');option.value=game.value;option.textContent=game.label;select.appendChild(option);});const otherOption=document.createElement('option');otherOption.value='other';otherOption.textContent='Board Games / Other';select.appendChild(otherOption);}function setupFilters(){const typeFilter=document.getElementById('filter-event-type');const gameFilter=document.getElementById('filter-event-game');const statusFilter=document.getElementById('filter-event-status');const resetBtn=document.getElementById('filter-reset-events');if(typeFilter)typeFilter.addEventListener('change',applyFilters);if(gameFilter)gameFilter.addEventListener('change',applyFilters);if(statusFilter)statusFilter.addEventListener('change',applyFilters);if(resetBtn){resetBtn.addEventListener('click',()=>{if(typeFilter)typeFilter.value='all';if(gameFilter)gameFilter.value='all';if(statusFilter)statusFilter.value='all';applyFilters();});}}function applyFilters(){const typeFilter=document.getElementById('filter-event-type');const gameFilter=document.getElementById('filter-event-game');const statusFilter=document.getElementById('filter-event-status');const type=typeFilter ? typeFilter.value : 'all';const game=gameFilter ? gameFilter.value : 'all';const status=statusFilter ? statusFilter.value : 'all';let filtered=allEvents.filter(event=>{const matchType=type==='all' || event.type===type;const matchGame=game==='all' || event.game===game;const matchStatus=status==='all' || event.calculatedStatus===status;return matchType && matchGame && matchStatus;});renderEvents(filtered);updateEventsCount(filtered.length);}function renderEvents(events){const grid=document.getElementById('events-grid');if(!grid)return;if(events.length===0){grid.innerHTML='<div class="no-results"><div class="icon">ðŸ“…</div><p>No events match your filters. Try resetting them!</p></div>';return;}grid.innerHTML=events.map(event=> buildEventCard(event)).join('');attachEventCardHandlers();}function buildEventCard(event){const eventType=eventsData.eventTypes.find(t=> t.value===event.type);const typeIcon=eventType ? eventType.icon : 'ðŸ“…';const typeLabel=eventType ? eventType.label : event.type;const typeColor=eventType ? eventType.color : '#D4A017';const status=STATUS_CONFIG[event.calculatedStatus] || STATUS_CONFIG.open;const eventDate=new Date(event.date);const dayName=eventDate.toLocaleDateString('en-US',{weekday: 'short'});const month=eventDate.toLocaleDateString('en-US',{month: 'short'});const day=eventDate.getDate();const year=eventDate.getFullYear();const startTime=event.time.substring(0,5);const endTime=event.endTime.substring(0,5);return ` <article class="event-card" data-event-id="${event.id}" role="button" tabindex="0"> <div class="event-card__header"> <h2 class="event-card__title"> ${typeIcon}${event.title}</h2> <span class="event-status ${status.class}"> ${status.icon}${status.label}</span> </div> <div class="event-card__date-badge" style="background: linear-gradient(135deg,${typeColor}0%,${typeColor}dd 100%);"> <div class="date-info"> <span class="date-day">${day}</span> <span class="date-month">${month}</span> <span class="date-year">${year}</span> </div> <div class="date-time"> <svg xmlns="http: <circle cx="12" cy="12" r="10"></circle> <polyline points="12 6 12 12 16 14"></polyline> </svg> ${startTime}- ${endTime}</div> </div> <div class="event-card__content"> <div class="event-card__meta"> <span class="event-meta-item"> <svg xmlns="http: <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"></path> <circle cx="9" cy="7" r="4"></circle> <path d="M23 21v-2a4 4 0 0 0-3-3.87"></path> <path d="M16 3.13a4 4 0 0 1 0 7.75"></path> </svg> Capacity: ${event.capacity}people </span> <span class="event-meta-item"> <svg xmlns="http: <rect x="3" y="4" width="18" height="18" rx="2" ry="2"></rect> <line x1="16" y1="2" x2="16" y2="6"></line> <line x1="8" y1="2" x2="8" y2="6"></line> <line x1="3" y1="10" x2="21" y2="10"></line> </svg> ${typeLabel}</span> </div> <p class="event-card__description">${event.description}</p> ${event.format ? ` <div class="event-card__details"> <span class="detail-badge">Format: ${event.format}</span> ${event.prizes ? `<span class="detail-badge">ðŸŽ ${event.prizes}</span>` : ''}</div> ` : ''}</div> </article> `;}function updateEventsCount(count){const counter=document.getElementById('events-count');if(!counter)return;counter.textContent=`${count}event${count !==1 ? 's' : ''}found`;}function showErrorMessage(){const grid=document.getElementById('events-grid');if(!grid)return;grid.innerHTML=` <div class="error-message"> <h3>Unable to Load Events</h3> <p>We're having trouble loading event information. Please try refreshing the page.</p> </div> `;}function initNavToggle(){const navToggle=document.getElementById('nav-toggle');const navLinks=document.getElementById('nav-links');if(!navToggle || !navLinks)return;navToggle.addEventListener('click',()=>{const isExpanded=navToggle.getAttribute('aria-expanded')==='true';navToggle.setAttribute('aria-expanded',!isExpanded);navLinks.classList.toggle('active');});}function initEventDetailModal(){const modal=document.getElementById('event-detail-modal');const closeBtn=document.getElementById('modal-event-close');if(!modal || !closeBtn)return;closeBtn.addEventListener('click',closeEventModal);modal.addEventListener('click',(e)=>{if(e.target===modal){closeEventModal();}});document.addEventListener('keydown',(e)=>{if(e.key==='Escape' && modal.style.display !=='none'){closeEventModal();}});}function attachEventCardHandlers(){document.querySelectorAll('.event-card').forEach(card=>{card.addEventListener('click',(e)=>{if(e.target.tagName==='A' || e.target.tagName==='BUTTON' || e.target.closest('a')|| e.target.closest('button')){return;}const eventId=parseInt(card.getAttribute('data-event-id'));const event=allEvents.find(e=> e.id===eventId);if(event){openEventModal(event);}});card.addEventListener('keydown',(e)=>{if(e.key==='Enter' || e.key===' '){e.preventDefault();const eventId=parseInt(card.getAttribute('data-event-id'));const event=allEvents.find(ev=> ev.id===eventId);if(event){openEventModal(event);}}});});}function openEventModal(event){const modal=document.getElementById('event-detail-modal');const modalBody=document.getElementById('modal-event-body');if(!modal || !modalBody)return;modalBody.innerHTML=buildEventDetailHTML(event);modal.style.display='flex';document.body.style.overflow='hidden';document.getElementById('modal-event-close').focus();}function closeEventModal(){const modal=document.getElementById('event-detail-modal');if(!modal)return;modal.style.display='none';document.body.style.overflow='';}function buildEventDetailHTML(event){const eventType=eventsData.eventTypes.find(t=> t.value===event.type);const typeIcon=eventType ? eventType.icon : 'ðŸ“…';const typeLabel=eventType ? eventType.label : event.type;const typeColor=eventType ? eventType.color : '#4A90E2';const status=STATUS_CONFIG[event.calculatedStatus] || STATUS_CONFIG.open;const eventDate=new Date(event.date);const fullDate=eventDate.toLocaleDateString('en-US',{weekday: 'long',year: 'numeric',month: 'long',day: 'numeric'});const startTime=event.time.substring(0,5);const endTime=event.endTime.substring(0,5);return ` <div class="event-detail"> <div class="event-detail__header"> <div class="event-detail__type" style="background-color: ${typeColor}20;color: ${typeColor};border-color: ${typeColor}50;"> ${typeIcon}${typeLabel}</div> <span class="event-status ${status.class}"> ${status.icon}${status.label}</span> </div> <h1 class="event-detail__title" id="modal-event-title">${event.title}</h1> <div class="event-detail__info"> <div class="event-info-item"> <svg xmlns="http: <rect x="3" y="4" width="18" height="18" rx="2" ry="2"></rect> <line x1="16" y1="2" x2="16" y2="6"></line> <line x1="8" y1="2" x2="8" y2="6"></line> <line x1="3" y1="10" x2="21" y2="10"></line> </svg> <div> <strong>Date</strong> <p>${fullDate}</p> </div> </div> <div class="event-info-item"> <svg xmlns="http: <circle cx="12" cy="12" r="10"></circle> <polyline points="12 6 12 12 16 14"></polyline> </svg> <div> <strong>Time</strong> <p>${startTime}- ${endTime}</p> </div> </div> <div class="event-info-item"> <svg xmlns="http: <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"></path> <circle cx="9" cy="7" r="4"></circle> <path d="M23 21v-2a4 4 0 0 0-3-3.87"></path> <path d="M16 3.13a4 4 0 0 1 0 7.75"></path> </svg> <div> <strong>Capacity</strong> <p>${event.capacity}people</p> </div> </div> ${event.format ? ` <div class="event-info-item"> <svg xmlns="http: <path d="M4 19.5A2.5 2.5 0 0 1 6.5 17H20"></path> <path d="M6.5 2H20v20H6.5A2.5 2.5 0 0 1 4 19.5v-15A2.5 2.5 0 0 1 6.5 2z"></path> </svg> <div> <strong>Format</strong> <p>${event.format}</p> </div> </div> ` : ''}</div> <div class="event-detail__description"> <h2>About This Event</h2> <p>${event.description}</p> </div> ${event.prizes ? ` <div class="event-detail__prizes"> <h2>ðŸŽ Prizes</h2> <p>${event.prizes}</p> </div> ` : ''}${event.image ? ` <div class="event-detail__image"> <img src="${event.image}" alt="${event.title}" /> </div> ` : ''}<div class="event-detail__actions"> ${event.calculatedStatus !=='past' ? ` <a href="${event.rsvpLink}" target="_blank" rel="noopener noreferrer" class="btn btn-primary btn-lg"> ${event.calculatedStatus==='full' ? 'Join Waitlist' : 'RSVP on Discord'}â†’ </a> ` : ` <span class="text-muted">This event has passed</span> `}</div> </div> `;}